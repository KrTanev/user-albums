import { Box, Grid } from "@mui/material";

export const FavouritesPage = () => {
  return (
    <Box>
      <Grid
        container
        rowSpacing={2}
        columnSpacing={{ xs: 1, sm: 1, md: 2 }}
      > test fav photo</Grid>
    </Box>
  );
};
